#!/usr/bin/env python3
"""Convenience script - use 'geotessera-registry serve' instead.

Usage:
    geotessera-registry serve /path/to/zarr/stores
    # or with uvx:
    uvx geotessera geotessera-registry serve /path/to/zarr/stores
"""
print("Use 'geotessera-registry serve <zarr_dir>' instead.")
print("Example: geotessera-registry serve /data/zarr --port 8000")
